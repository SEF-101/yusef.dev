export const metadata = {
  title: "SEF101 Media & VPN Access Instructions",
};

export default function ServerConnectLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      {children}
    </>
  );
}
